{
  "name": "next-hash-spa-test",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "export": "next build && next export && rm -rf docs/* && cp -r out/* docs && touch ./docs/.nojekyll",
    "deploy": "npm run export && npx gh-pages -d docs",
    "serve": "serve -l 3000 docs",
    "start": "next start"
  },
  "dependencies": {
    "next": "13.4.10",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "react-router-dom": "6.12.1",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "gh-pages": "5.0.0",
    "serve": "14.2.0"
  }
}
